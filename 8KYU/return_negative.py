def make_negative( number ):
    if number <= 0:
        return number
    else:
        number *= (-1)
        return number

    def make_negative_2(number):
        if number <= 0:
            return number
        else:
            return -number

    def make_negative_3(number):
        if number > 0:
            return -number
        else:
            return number
